[variables]
NODE_VERSION = "18"
NPM_VERSION = "9"

[phases.setup]
nixPkgs = ["nodejs_18", "npm"]

[phases.install]
cmds = ["npm install --verbose"]

[phases.build]
cmds = ["npm run build --verbose"]

[start]
cmd = "npm start"
