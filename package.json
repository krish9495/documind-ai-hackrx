{
    "name": "documind-ai-hackrx",
    "version": "1.0.0",
    "description": "DocuMind AI - HackRX 2024 Submission",
    "private": true,
    "scripts": {
        "install:app": "cd query-zenith && npm install",
        "build:app": "cd query-zenith && npm run build",
        "start:app": "cd query-zenith && npm start",
        "dev:app": "cd query-zenith && npm run dev",
        "start": "cd query-zenith && npm start"
    },
    "engines": {
        "node": ">=18.0.0",
        "npm": ">=9.0.0"
    }
}