{
  "build": {
    "builder": "nixpacks",
    "buildCommand": "cd query-zenith && npm install && npm run build"
  },
  "deploy": {
    "startCommand": "cd query-zenith && npm start",
    "restartPolicyType": "always"
  }
}
